// styles/stitches.config.js
import { createStitches } from '@stitches/react';

export const { styled, globalCss } = createStitches({
    theme: {
        space: {
            paddingTop: '20px',
            paddingLeft: '20px',
        },
    },
});



